// Custom Cypress commands are declared here to reuse logic across tests.
// This file is imported automatically by cypress/support/e2e.js.

Cypress.Commands.add('loginIfNeeded', () => {
  // Placeholder command for future login actions if the application requires authentication.
  // Example: cy.visit('/login'); cy.get('#email').type(...);
});
